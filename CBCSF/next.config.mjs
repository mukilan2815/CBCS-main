/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["admission.kahedu.edu.in", "metaverse-portal.vercel.app"],
  },
};

export default nextConfig;
